/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.w3schools.com'], // Add the domain here
        domains: ['fakestoreapi.com'], 
      },
};

export default nextConfig;
